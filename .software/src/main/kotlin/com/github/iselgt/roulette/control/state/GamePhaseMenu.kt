package com.github.iselgt.roulette.control.state

/**
 * This interface defines the contract for a game phase menu.
 * Each game phase should implement this interface to provide its own menu functionality.
 */
interface GamePhaseMenu {
    fun menu()
}